name: Stability Check

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  validate:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: expo

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: TypeScript check
        run: bunx tsc --noEmit

      - name: Lint
        run: bunx expo lint

      - name: Run handoff validation
        run: bun scripts/validate-handoff.js

      - name: Run repository health check
        run: bun scripts/repo-health-check.js

      - name: Run production preflight
        run: bun scripts/production-preflight.js

      - name: Run final validation
        run: bun scripts/final-validation.js

      - name: Export web bundle
        run: bunx expo export --platform web

      - name: Export Android bundle
        run: bunx expo export --platform android
